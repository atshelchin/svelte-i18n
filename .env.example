# Environment configuration for svelte-i18n demo app

# Build mode configuration
# Options: 'static' | 'ssr' | 'auto' (default)
# - static: For GitHub Pages deployment (prerender = true, ssr = false)
# - ssr: For server deployment with SSR support (prerender = false, ssr = true)
# - auto: Automatically detect based on environment
VITE_BUILD_MODE=auto

# Example usage:
# Local development (SSR enabled):
#   npm run dev
#
# Build for GitHub Pages:
#   npm run build:ghpages
#   or
#   npm run build:static
#
# Build with SSR support:
#   npm run build:ssr
#
# Default build (auto-detect):
#   npm run build